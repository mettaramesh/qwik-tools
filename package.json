{
  "name": "qwik-tools",
  "version": "1.0.0",
  "description": "Qwik - Developer Tools SPA for GitHub Pages",
  "type": "module",
  "browserslist": [
    "last 2 Chrome versions",
    "last 2 Edge versions",
    "not dead"
  ],
  "scripts": {
    "dev": "vite",
  "build": "vite build",
    "preview": "vite preview",
    "audit": "npm audit --audit-level=high",
    "osv": "npx osv-scanner --lockfile=package-lock.json"
  },
  "dependencies": {
    "xmllint-wasm": "^5.0.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "vite-plugin-wasm": "^3.5.0"
  }
}
